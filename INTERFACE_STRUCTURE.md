# VividWrite 2.0 界面结构

## 🎯 新的按钮层级结构

### 主界面布局
```
┌─────────────────────────────────────────────────────────────┐
│                    Vividwrite2.0                            │
├─────────────────────────────────────────────────────────────┤
│  [Flowchart]  [Feedback]                                    │
│                                                             │
│  ┌─────────────────┐  ┌─────────────────────────────────┐   │
│  │                 │  │                                 │   │
│  │   Original      │  │        Content Area             │   │
│  │   Image         │  │                                 │   │
│  │                 │  │  (显示选中的内容)                │   │
│  ├─────────────────┤  │                                 │   │
│  │                 │  │                                 │   │
│  │   Writing       │  │                                 │   │
│  │   Area          │  │                                 │   │
│  │                 │  │                                 │   │
│  │  [分析文本]      │  │                                 │   │
│  └─────────────────┘  └─────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 按钮交互流程

#### 1. 初始状态
```
[Flowchart] [Feedback]
```
- 默认显示 Flowchart 内容
- Feedback 按钮未激活

#### 2. 点击 Feedback 后
```
[Flowchart] [Feedback] ← 激活状态
    [Visual Feedback] [Revision Suggestions]
```
- Feedback 按钮变为激活状态（蓝色）
- 显示两个子按钮（绿色主题）
- 子按钮有左边框和缩进，表示层级关系

#### 3. 选择子选项后
```
[Flowchart] [Feedback] ← 保持激活
    [Visual Feedback] ← 选中状态
```
- 子按钮变为选中状态（绿色）
- 内容区域显示对应的内容

## 🎨 视觉设计特点

### 颜色方案
- **主按钮未激活**: 浅灰色背景 (#f8f9fa)
- **主按钮激活**: 蓝色背景 (#007bff)
- **子按钮未激活**: 浅灰色背景 (#e9ecef)
- **子按钮激活**: 绿色背景 (#28a745)

### 层级表示
- **子菜单缩进**: 左侧增加 1rem 的 padding
- **左边框**: 3px 蓝色边框表示层级关系
- **字体大小**: 子按钮稍小 (0.9rem)

## 🔄 交互逻辑

### 状态管理
```javascript
const [rightContent, setRightContent] = useState("Flowchart");
const [showFeedbackSubmenu, setShowFeedbackSubmenu] = useState(false);
```

### 点击处理
1. **点击 Flowchart**: 
   - 设置内容为 "Flowchart"
   - 隐藏 Feedback 子菜单

2. **点击 Feedback**:
   - 显示子菜单
   - 如果当前是 Flowchart，默认显示 "Visual Feedback"

3. **点击子按钮**:
   - 设置对应的内容
   - 保持子菜单显示

## 📱 响应式设计

### 按钮布局
- 主按钮使用 `display: flex` 水平排列
- 子按钮在激活时显示，使用相同的 flex 布局
- 按钮间距统一为 1rem

### 内容区域
- 内容区域根据选中的按钮显示不同内容
- 支持滚动，适应不同长度的内容

## 🎯 用户体验

### 直观的导航
- 两级按钮结构清晰表达功能层级
- 颜色编码帮助用户理解当前状态
- 视觉层级通过缩进和边框表示

### 功能分离
- Flowchart 完全独立，不依赖分析结果
- Feedback 功能集中管理，便于扩展
- 子功能清晰分离，避免界面混乱

### 状态保持
- 用户选择的状态在切换时保持
- 子菜单状态合理管理，避免意外关闭
